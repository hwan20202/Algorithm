void recursion()
{
    for (int i = 0; i < 8; i++)
    {
        if (v->at(i) == false)
            continue;
        if (i > 1 && bad(s[i - 1]), i))
            continue;
        if (cur == 7)
            ret++;

    }
}